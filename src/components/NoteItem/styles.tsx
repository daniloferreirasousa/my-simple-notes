import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        padding: 16,
        backgroundColor: '#444',
        marginBottom: 5,
        borderRadius: 5
    },
    title: {
        fontSize: 16,
        color: '#fff'
    }
});

export default styles;